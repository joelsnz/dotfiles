" config
set tabstop=2 "spaces that represents a tab
set shiftwidth=2 "spaces used for auto indent
set expandtab "convert tabs to spaces

set number "line numbers
set relativenumber "0 always on cursor

syntax enable
set termguicolors
